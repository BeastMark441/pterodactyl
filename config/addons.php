<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Addon Lifecycle Hooks
    |--------------------------------------------------------------------------
    |
    | Если эта функция включена, панель запускает скрипты перехвата, которые аддоны размещают в разделе
    | "addons/<имя>/перехваты/<событие>" во время событий жизненного цикла, таких как последующая установка
    | (смотрите команду p:environment:addons:run-hooks). Эти скрипты запускаются с правами
    | вызывающего процесса — часто root во время обновления — поэтому
	| включайте это только в том случае, если вы доверяете каждому установленному дополнению.
    |
    */
    'hooks_enabled' => env('ADDONS_HOOKS_ENABLED', false),
];